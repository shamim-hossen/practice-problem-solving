#include<bits/stdc++.h>
using namespace std;

int main() {
    int N;
    cin >> N;

    // Print the first 10 multiples of N
    for (int i = 1; i <= 10; ++i) {
        cout << N * i << endl;
    }

    return 0;
}
